var a = 3;

const sumar = (x, y) => {
  const a = x;
  const b = y;
  console.log(a + b);
  return a + b;
};

const add = (x, y) => {
  const a = x;
  const b = y;
  console.log(a + b);
  return a + b;
};
